/**
 * Fitness Frames - Level 0 Layout Shells
 * 
 * Frames are pure layout components that define the physical screen regions
 * without knowing what goes inside them. They accept children/render props
 * for each slot.
 * 
 * @module frames
 */

export { default as FitnessFrame } from './FitnessFrame.jsx';
export { default as FitnessPlayerFrame } from './FitnessPlayerFrame.jsx';
