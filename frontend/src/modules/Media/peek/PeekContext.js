// frontend/src/modules/Media/peek/PeekContext.js
// Filled by PeekProvider (peek phase). Declared here so useSessionController
// can resolve remote controllers without a hard dependency on the provider.
import { createContext } from 'react';

export const PeekContext = createContext(null);
