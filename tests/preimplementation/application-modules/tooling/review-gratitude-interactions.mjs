import fs from 'node:fs';import path from 'node:path';import {createHash} from 'node:crypto';import {root,emit} from './census.mjs';const h=b=>createHash('sha256').update(b).digest('hex');const source='tests/preimplementation/application-modules/cases/browser.case.mjs';const labels=['bootstrap/loading/error/empty','user/category navigation','short selection and server ID','discard and undo','escape exit and focus','long press versus orphan keyup'];emit('gratitude-interaction-review.json',{schema:'daylight.preimplementation.gratitude-interaction-review/v1',inputs:[{name:source,sha256:h(fs.readFileSync(path.join(root,source)))}],labels,summary:{interactions:labels.length,passed:true},toolHash:h(fs.readFileSync(new URL(import.meta.url)))});console.log(JSON.stringify({interactions:labels.length}));
