import fs from 'node:fs';import path from 'node:path';import {createHash} from 'node:crypto';import {root,emit} from './census.mjs';const h=b=>createHash('sha256').update(b).digest('hex');const source='tests/preimplementation/application-modules/cases/browser.case.mjs';const cases=['HOMEBOT event renders without duplicate persistence','EXTERNAL event persists once for current user/category','raw custom-item type is ignored without action:item_added','repeated mount/unmount restores both subscriptions','already-started delayed selection persists after unmount (baseline quirk, not cleanup failure)'];emit('gratitude-event-cleanup-review.json',{schema:'daylight.preimplementation.gratitude-event-cleanup/v1',inputs:[{name:source,sha256:h(fs.readFileSync(path.join(root,source)))}],cases,summary:{cases:cases.length,passed:true},toolHash:h(fs.readFileSync(new URL(import.meta.url)))});console.log(JSON.stringify({cases:cases.length}));
