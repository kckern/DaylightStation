/** Exact future resolver/build projection inventory; current configuration is read only. */
import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {root, packet, emit} from './census.mjs';
const read = name => JSON.parse(fs.readFileSync(path.join(packet, name)));
const topology = read('package-topology-review.json');
const exists = file => assert.ok(fs.existsSync(path.join(root, file)), 'Missing projection source: ' + file);
const projections = [
  {id:'node-root', files:['package.json','package-lock.json'], disposition:'change', proposal:'Keep current # aliases only for unmigrated source. Add workspace membership for the 12 selected facet manifests; do not add a catch-all @daylight imports alias. Native resolution of public package exports is mandatory.', acceptance:['CASE-PKG-SIBLINGS','CASE-PKG-PRIVATE','missing-export native red']},
  {id:'node-backend', files:['backend/package.json','backend/package-lock.json'], disposition:'change', proposal:'Retain type:module and legacy # imports during mixed-tree rehearsal. Do not map @daylight/* to source; server facets use package exports and relative owner-private imports only. Freeze canvas/uuid/timezone package graph with root lock change.', acceptance:['CASE-PKG-NATIVE','CASE-PKG-ACTUAL-TIMEZONES','native HTTP/rendering receipts']},
  {id:'node-frontend', files:['frontend/package.json','frontend/package-lock.json'], disposition:'change', proposal:'Retain type:module. Browser facets resolve public workspace packages natively; declare React/Mantine peers and date-fns direct dependency as selected by dependency identity review.', acceptance:['CASE-PKG-REACT','browser candidate resolver/identity test']},
  {id:'facet-manifests', files:topology.packages.map(item=>item.path+'/package.json'), disposition:'add', proposal:'Create exactly the 12 private local facet manifests from topology. Each has type:module, narrow exports, workspace-local facade dependencies and the classified direct/peer dependencies; no owner-root manifest or wildcard export.', acceptance:['CASE-PKG-NONOVERLAP','CASE-PKG-SIBLINGS','CASE-PKG-PRIVATE']},
  {id:'vite', files:['frontend/vite.config.js'], disposition:'change', proposal:'Retain @/gaming/shared aliases for unmigrated source and existing React/Sass/port/proxy behavior. Add no @daylight source alias; resolve package exports through workspace installation. Set dedupe for react, react-dom, @mantine/core, @mantine/hooks and @mantine/notifications; allow only platform/, modules/, capabilities/ and shared/contracts/ as required workspace source roots, never a repository-wide fs allow.', acceptance:['browser clean-install build','React/Mantine duplicate-instance red','workspace external-root/HMR fixture']},
  {id:'vitest', files:['vitest.config.mjs'], disposition:'change', proposal:'Retain legacy aliases and current test exclusions until each old import is removed. Candidate mode resolves @daylight packages via installed workspace exports, not aliases or another checkout; add matching React/Mantine dedupe. Keep root-canvas alias test-only and retain native canvas proof separately.', acceptance:['CASE-PKG-REACT','CASE-PKG-NATIVE','missing-workspace-root red']},
  {id:'jest-root', files:['jest.config.js'], disposition:'change', proposal:'Retain legacy # mappers for unmigrated source. Do not add a broad @daylight/*→private-path mapper: migrated tests must resolve public packages natively or run under their owning selected runner.', acceptance:['CASE-PKG-PRIVATE','runner missing-root negative']},
  {id:'jest-backend', files:['backend/jest.config.js'], disposition:'retain-with-assignment', proposal:'Keep no alias map/transform. Assign moved backend tests explicitly to their selected runner; do not duplicate root mapper merely for package symmetry.', acceptance:['runner-review selected discovery','moved-test ownership check']},
  {id:'sass', files:['frontend/vite.config.js','frontend/src/modules/AppContainer/Apps/Gratitude/Gratitude.scss'], disposition:'retain-options-change-resolution', proposal:'Retain modern compiler options. Gratitude SCSS moves with its web surface and keeps local relative asset imports; add no global Sass load path or alias.', acceptance:['Gratitude surface build and SVG/style closure test']},
  {id:'test-discovery', files:['scripts/gate-vitest.mjs','tests/_infrastructure/harnesses/isolated.harness.mjs','tests/_infrastructure/frontend-env.mjs','frontend/src/test-setup.js'], disposition:'change-if-moved-tests', proposal:'Add only approved facet test roots and canonical-path deduplication under IMP-BASE.03. Preserve runner identities and fail if an expected moved root is absent.', acceptance:['runner missing-root red','original named-population comparison']},
  {id:'watch', files:['package.json','nodemon.json'], disposition:'change', proposal:'Expand server watch roots only to selected platform/modules/capabilities server sources; preserve .env watch, extension list and browser HMR ownership. Do not watch generated evidence/output.', acceptance:['one representative restart per approved server root','duplicate-loop red']},
  {id:'editor', files:['.vscode/settings.json'], disposition:'retain', proposal:'No editor alias or ts/js config is selected. Retain watch/search exclusions; add a path only after a concrete observed editor failure.', acceptance:['editor resolution is non-blocking until observed failure']},
  {id:'docker-context', files:['docker/Dockerfile','.dockerignore','docker/.dockerignore','docker/entrypoint.sh'], disposition:'change-copy-only', proposal:'Before npm ci, copy root/backend/frontend locks plus the 12 facet manifests. In source stage copy selected platform/, modules/, capabilities/ and shared/contracts/ roots. Retain native canvas build packages, font/resource closure, frontend build and entrypoint contract; do not add unused roots or runtime mounts.', acceptance:['clean image context/resource closure','native canvas/font probe','entrypoint candidate resolution']},
  {id:'locks-and-native-scripts', files:['package-lock.json','backend/package-lock.json','frontend/package-lock.json','docker/Dockerfile','package.json','nodemon.json'], disposition:'change', proposal:'Generate all three locks atomically from the approved workspace/manifests. Preserve Docker native build prerequisites and current backend:dev/nodemon behavior while adding selected server roots. Canvas version/ABI discrepancy is a hard gate.', acceptance:['offline nested install + clean reinstall','CASE-PKG-NATIVE','Linux image gate']}
];
for (const projection of projections) for (const file of projection.files) {
  if (!file.includes('/package.json') || !file.startsWith('platform/') && !file.startsWith('modules/') && !file.startsWith('capabilities/') && !file.startsWith('shared/')) exists(file);
}
assert.equal(new Set(projections.map(item => item.id)).size, projections.length);
assert.ok(projections.every(item => item.files.length && item.proposal && item.acceptance.length));
emit('resolver-build-projection-review.json', {
  schema:'daylight.preimplementation.resolver-build-projection-review/v1', status:'all selected resolver/build projection surfaces enumerated; no configuration has been applied', projections,
  workspaceMembership: topology.packages.map(item => ({name:item.name,path:item.path,manifest:item.path+'/package.json'})),
  forbidden:['No production @daylight source alias that bypasses package exports.','No broad Vite server.fs.allow root.','No public package wildcard export.','No root-canvas production fallback.','No invented jsconfig/tsconfig/editor alias.'],
  limits:['Future patch content/lock hashes and actual browser/image execution remain implementation gates. This inventory does not change resolver, build, watch, install or editor state.']
});
process.stdout.write(JSON.stringify({projections:projections.length,workspaces:topology.packages.length,output:'resolver-build-projection-review.json'})+'\n');
