import fs from 'node:fs';import path from 'node:path';import {createHash} from 'node:crypto';import {root,emit} from './census.mjs';const h=b=>createHash('sha256').update(b).digest('hex');
const files=['tests/preimplementation/application-modules/configs/vitest.config.mjs','tests/preimplementation/application-modules/tooling/run-node.mjs','tests/preimplementation/application-modules/tooling/run-vitest.mjs','tests/preimplementation/application-modules/README.md'];const text=Object.fromEntries(files.map(n=>[n,fs.readFileSync(path.join(root,n),'utf8')]));const checks={explicitRoot:text[files[0]].includes('PRE_RUN_ROOT'),allowlistedMode:text[files[0]].includes('Explicit allowlisted preparation Vitest mode required'),noSetup:text[files[0]].includes('setupFiles:[]'),noWatch:text[files[0]].includes('watch:false'),noController:text[files[3]].includes('Never run the household controller')};emit('runner-configuration-review.json',{schema:'daylight.preimplementation.runner-configuration-review/v1',inputs:files.map(name=>({name,sha256:h(fs.readFileSync(path.join(root,name)))})),checks,summary:{passed:Object.values(checks).every(Boolean)},toolHash:h(fs.readFileSync(new URL(import.meta.url)))});console.log(JSON.stringify(checks));
