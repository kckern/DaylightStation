# Test fixture for service resolution
plex:
  docker: plex
  test-env: localhost

homeassistant:
  docker: homeassistant
  test-env: localhost

homeassistant-secondary:
  docker: ha-secondary
  test-env: 10.0.0.20

mqtt:
  docker: mosquitto
  test-env: localhost
