# Ordinary Media runtime acceptance — bounded brief

- Keep F2 as foundation only: it imports the correlator directly, uses `/media` in both tabs, asserts zero `<video>`, and blocks every `/api/v1/device/**` write.
- The ordinary sender path is `ResultRow` Play/Add → `DispatchTargetPicker` → `DispatchProvider` → **GET** `/api/v1/device/:deviceId/load?...` (`backend/src/4_api/v1/routers/device.mjs`) → `DeviceContentDispatchService` → real `WakeAndLoadService`/registered device adapter.
- The real receiver path is `/screen/:screenId` → `ScreenRenderer` config (`GET /api/v1/screens/:screenId`) → `useScreenCommands`/`CommandAckPublisher` → `ScreenActionHandler`/foreground Player; it is distinct from Media `useExternalControl` and must own native video/audio plus returned state.
- `media-redesign-server.mjs` only owns branch `/play` and Plex mint routes; its Vite `/api` and `/ws` proxies send `/device/:id/load` and receiver WS to the upstream real application, where the configured device is physical.
- Therefore no smallest fixture-only receiver extension exists: a browser opened at a configured `/screen/...` can claim that real fleet id but cannot replace the upstream `DeviceService` adapter/WakeAndLoad composition; a local F2 bus cannot exercise the real service.
- Implemented acceptance composition (not product code): `media-ordinary-device-fixture.mjs` owns one virtual registered device, local device router + `WakeAndLoadService`/real `WebSocketContentAdapter`/event bus/liveness, and a read-through living-room config with its guardrail renamed only to that device.
- The ordinary warm path is WakeAndLoad's positive subscriber+liveness WS-first branch (the adapter is the real cold/fallback path); the runtime story polls that transport proof, then proves advancing unpaused native media plus actual published owner/revision/queue state and truthful `Playing`/`Added` tray copy.
- Startup after the reviewed clean build is composed: start that fixture server, set `BASE_URL` to its printed URL, then `npx playwright test tests/live/flow/media/media-app-browser-control.runtime.test.mjs --workers=1`; do not use the accepted-preview `1bc` artifact as evidence.
