# Media redesign execution

**Objective:** Build and verify all 82 stories, starting with P0, with no presumed functional baseline.
**Spec:** `2026-09-14-media-app-redesign-requirements.md` and taxonomy §3.
**Ownership:** `2026-09-14-media-app-story-implementation-map.md`.

## Global constraints

- Every story begins unverified. Unit passes are supporting evidence, not end-to-end acceptance.
- Demonstrate a behavioral failure before changing implementation; preserve working behavior with regression tests.
- Validate interaction → destination → command → actual player → returned state → accurate UI and feedback.
- Office screen is the only authorized physical test screen. Browser contexts may simulate additional screens. Preserve/restore office state; do not disturb other kiosks.
- P0 first, then P1/P2. Minimum undo moves into P0; expires 10 seconds after tap, cancels pending dispatch, returns live sources to live. Cross-device restoration and notes remain P1.
- Keep the bridge-owned media node stable across navigation; Content modules never import Media modules.
- Structured logs, design-system tokens, existing local/remote controller seam, additive API compatibility.
- Root agent owns tracking docs and end-to-end harness. Task workers own only their named implementation scope. Do not spawn subagents from workers. Commit only owned files; do not deploy from workers.

## Parallel execution policy

The owner requested model-appropriate parallel orchestration during implementation. Split parent tasks into independently testable slices with exclusive file ownership; a completed slice does not complete its parent story. Keep up to three worker lanes active alongside the coordinator. Use Luna for fully specified mechanical work, Sol for bounded UI behavior, Terra for multi-file integration, and Astra for architecture or difficult playback debugging. Reviews go to a worker other than the implementer.

| Lane / dependency | Work | Integration gate |
|---|---|---|
| Playback | Task2 native ownership, stream timeline, actual pause/seek/focus/stop | Independent scoped review and serial real-decoder suite |
| Aim | Task3a persistence/idle lifetime and observed steering activity | Real reload/idle journey; stable activity/identity contract before moves |
| Search | Task5a keep-open/history repair using current action interfaces | Real phone search journey; Task4 one-off actions remain an explicit dependency |
| Architecture | Task3b safe-move API/receiver transaction design, initially read-only | Failed-start/lost-ack/source-changed REDs before implementation |

Next-wave partition: Task2b measures and repairs the startup/stream operational
gate; Task3a review corrections stay with their original implementer; Task4a
repairs the shared combobox action boundary; F2 (early Task7) wires identified
live browser control and validated command/ack round trips. F1a (early Task4)
then establishes authoritative queue capture and owner-issued revisions,
followed by F1b adoption/conditional Stop, F3 native-confirmed execution and the
move coordinator. F2 is independent of Player/Plex files; F1's shared Player
edits require an explicit lease against Task2b. Browser naming/origin, photo
readiness and live-edge moves remain explicit later consumers, not inferred
from successful AV or transport-only tests.
| Subsequent waves | Task4 verbs/queue/undo, remainingTask5 browse/search, Task7 registry/origin; thenTask6 controls, Task8 outcomes andTask9 recovery | Assign only disjoint files; freeze shared command/identity contracts before dependent consumers |
| Acceptance and later priorities | Task10 parity/budgets, then exact P1/P2 slices below | Full criterion evidence; never promote component test counts to story acceptance |

The coordinator owns shared reference/tracking docs, browser/API harness composition and cross-slice verification. Workers request ownership before widening scope. Serialize git staging/commits with an explicit coordinator lease and serialize media-heavy browser suites; unit work can proceed concurrently. Preserve known failing journeys until their owning slice repairs them. Record model, task, file ownership, RED/GREEN evidence, review state and unresolved dependencies in the execution ledger.

## Task 1: Four immediate defects

Implement handoff P0 step 1 with test-first behavior checks. Read its Step 1 plus original AC for PLACE.2b, STEER.7a, RELY.6a, PLACE.1a/STEER.1b.

1. D1: Phone destination picker must offer This device; selecting it clears remote targets immediately, with valid accessible UI. Preserve explicit one-off dispatch pickers; This device is the global aim operation, not a fake remote target.
2. D2: Stop keeps the queue accessible through MiniPlayer in ready state; clicking it opens Now Playing/Queue, Play restarts retained queue, clear/reset removes the empty handle. Avoid undefined item accesses.
3. D3: Store exact attempt parameters per dispatchId and expose retry(dispatchId); update tray rows and search failure notices to retry their own target/content/options/snapshot, not the last attempt. A retry of one failed multi-target row must not replay successful siblings. Retain compatibility only for actual remaining callers. Explicit retry bypasses failed-attempt dedupe; repeated taps on an in-flight retry must not duplicate it.
4. D4: Remove implicit peek destination override in all search dispatch functions: single play, collection play/shuffle, collection append. Displayed aim controls destination. Opening/closing Remote never changes aim.

Owned files: Media/cast dispatch/provider/picker files and tests; Media/search/useContentDispatch.js and tests; Media/shell/MiniPlayer.jsx and tests. Do not change PlayerBridge, LocalSessionController, or root-owned ledger/harness. Root will cover live acceptance separately. Add original story IDs to test names. Test through real providers/controllers where practical; mock only external transport. Run focused tests RED then GREEN, related existing tests once after implementation. Update a concise factual reference-doc paragraph for this slice; no claim that full stories passed.

Run `npx vitest run <changed-test-files> --maxWorkers=2`; commands run from this worktree. Main baseline captured separately.

Browser acceptance exposed an additional D1 junction failure: on a phone, the destination modal opens but the Search Mode layer intercepts ordinary taps on its device buttons. Fix the destination-modal/search-layer integration as part of D1; ownership extends to DestinationLine.jsx and necessary search/cast styling. Preserve ordinary pointer and keyboard interaction; do not use forced or synthetic browser clicks. Root-owned `tests/live/flow/media/media-app-aim-journey.runtime.test.mjs` is the RED and GREEN browser check. Root reruns it after implementation.

## Task 2: Disclosure Day playback wiring

Reproduce the real Player journey from search with metadata lacking duration. Reconcile resolved metadata, currentTime, paused/playing state, duration and media capabilities back into the session without remounting Player. Repeated Play on running content must not arm a false startup stall. Seek and progress need real media evidence, including DASH/shadow DOM. Focused playback and Stop must be reachable. Test initial play, seek ±10s/scrub, pause/resume, repeat Play, host expand/shrink, stop/retained queue. Root prepares the browser scenario before dispatching this task.

Confirmed browser RED: `tests/live/flow/media/media-app-playback-journey.runtime.test.mjs` starts the actual movie via the real catalog and ordinary input. Video advances while the Seek slider has maximum zero, unknown length, and disabled interaction. Search metadata lacks duration/format; Player's real progress includes duration but PlayerBridge ignores it. Today’s logs also show Play on already-running content changing session state to loading, then stalled at the watchdog boundary while video continues rendering. A normal pause/resume browser journey currently passes and must remain passing.

Owned implementation: Media/session PlayerBridge, LocalSessionController, reducer/store interfaces and their tests; Media/shell NowPlayingView, SeekBar, TransportBar, related styling and tests. MiniPlayer integration edits are allowed after Task 1 review. Root owns browser harness and tracking docs. Read actual Player media-access/progress contracts; preserve the shared Player for other apps. Add unit RED before implementation for metadata enrichment without media remount, repeated Play while playing, actual paused/playing reconciliation, and DASH media access. Cover stale callbacks from old content and zero/unknown/live duration without inventing seekability.

Provide an accessible `Expand video` control in Now Playing and `Shrink video` while expanded, with focused rendering and reachable transport/Stop. Expansion/shrinking and host navigation must retain the same advancing media element. Use the existing focused shader capability and design-system styling. Avoid a new parallel player. Full-screen mode may fill the app viewport; browser fullscreen is optional. Changes to metadata must not become a new playback identity or reset position. Ensure real-player duration and progress update the visible bar, and scrub changes the actual position. Do not optimistically claim playback has changed when only a command has been issued.

Run focused RED/GREEN unit coverage and existing relevant session/shell tests. Root reruns real browser acceptance, including unchanged normal pause/resume and stop/retained-queue cases. Update a factual reference paragraph and write task report with exact commands, output, red evidence, changed interfaces and concerns. Commit only owned implementation/test/reference files; no deploy.

## Task 3: Shared aim, idle lifetime and safe moves

Execution dependency update: Task2's reviewed native fixes are retained, but seven serial journeys still fail initial decoder readiness. Corrective Task2b owns measured stream-decision/mint/MPD/decoder/recovery diagnosis and the globalDASH sustained-capacity/quality release gate. It must close those failures before actual playback baseline is accepted; no silent sixth review round or timeout-only acceptance. Safe moves additionally require the authoritative receiver queue/owner-revision foundation (earlyTask4), native-confirmed handoff executor, and distinct live browser control registration/result relay (earlyTask7). Implement these foundations in disjoint lanes before wiring move UI; receipt acknowledgments and cached empty queues are not sufficient.

Read handoff Step 2 and original PLACE.1a, PLACE.2a/2b, PLACE.5a/6a/7a/8a criteria. Deliver one shared aim model and label, not independent destination state in each surface. Preserve additive adapters for existing cast consumers until the common item-actions migration. Required model: selected target IDs, remembered stop-or-keep choice and persisted activity time; idle timeout 2 hours. Restore synchronously without first persisting a blank default. Track real interaction; pause the idle clock while the selected screen is playing an item this device sent or is actively steering. Reload after expired idle selects this device. Opening Remote alone never changes aim.

Replace duplicated label/chip behavior with one accessible aim surface used in search, browse, details, start and controls' add entry. This device always available at phone/tablet/laptop sizes. Show name/room, busy warning and stop-or-keep consequence before Play. Opening or selecting aim alone must send no playback. Use fleet state rather than treating an absent snapshot as confirmed idle. Origin/naming interfaces are additive dependencies on Task 7; do not mark those AC accepted with guessed origin or fabricated device names.

Moves must preserve source until destination actually acknowledges starting the same queue/item at the current position (live starts fresh). Current useTakeOver calls claim, which stops the source before local adoption: demonstrate a failed-local-start RED and repair the transaction. Protect against stale source identity before stopping it; never stop newer playback after a slow handoff. Failure must explain what happened and leave the original usable. Respect the remembered keep/stop choice for Move to; expose moves in the handle, house rows and full controls. Preserve media-node identity for local playback, including Task 2 metadata enrichment. Use the controller and existing API seams, adding backward-compatible acknowledgment/transaction fields where required; no parallel Player or separate aim route.

Ownership: aim provider/hook/label and consumer wiring; move hooks and transaction/controller/API tests; necessary device session application/API contract additions; receiver-side command/ack/identity guard changes required for safe moves; factual reference docs. Root owns ledger and runtime harness. Receiver seams include `screen-framework/commands/useScreenCommands.js`, `actions/ScreenActionHandler.jsx`, publishers and shared media contracts; any added precondition must survive every hop and be checked by the actual playback owner before applying Stop. Receipt of an envelope is not proof that destination playback started. Unit RED/GREEN must cover idle boundary/reload/active-playing exemption, all label consumers, one-off state isolation, move failed startup/late response/source changed, queue/position and live-edge preservation. Browser acceptance must observe real source and destination players in isolated contexts or office only; intermediate unit success is not full story acceptance. Run changed tests and relevant controller/dispatch regressions; backend edits require layer and composition gates. Write report and commit scoped files, no deploy.

## Task 4: Shared item actions, receiver queue parity and minimum undo

Read handoff Step 3, RQ-PLAY-01–07, RQ-FIND-09/10 and taxonomy PLAY.1a/1b/2a/3a/5a/6a/7a, FIND.8a/8b, PLACE.3a/3b and RELY.4a. Build one item-actions contract and one aim-aware dispatcher for all existing surfaces (search, browse, details, recent, collection headers). Later P1/P2 surfaces must consume this contract rather than inventing verbs. Keep Content props additive and independent of Media imports. Opening a collection or Details must not start playback; cameras/single photos show locally with an explicit Show on… alternative. Preserve the current view when starting playback.

Single-item Play starts now with `clearRest:false`; collection Play replaces in natural order with shuffle off; collection Shuffle replaces and turns shuffle on. Respect known unfinished-part information without pretending P1 household spot resolution is complete. Play next uses order-added semantics (`addUpNextMany` today); repeated additions preserve their order. A press-and-hold secondary action offers At the very front (`playNextMany` today), with a keyboard-accessible equivalent. Add to queue appends naturally ordered items without interrupting current playback. Every applicable item/collection has these verbs plus explicit one-off Play on… / Add to queue on… and Details. One-off selection leaves the shared aim unchanged and uses its remembered stop-or-keep preset. Confirmation names item/collection, destination, count/position and actual outcome. Preserve exact-attempt retry semantics from Task1.

Root browser RED at the menu junction: in desktop search, an ordinary More actions click on the paused current movie clears the query and resumes playback instead of exposing Add to Queue. This blocks the same-title duplicate queue jump journey before its generation assertion. Preserve and repair `tests/live/flow/media/media-app-queue-journey.runtime.test.mjs`; do not bypass the broken menu with a synthetic click or direct controller mutation. Once the menu is repaired, verify add preserves playback, then selecting the second same-title queue entry actually starts that entry at zero. Task2 provides the underlying generation/position repair, but this whole junction remains unaccepted until that browser test passes.

Support local and screen receiver add, reorder, remove, jump and clear through their actual playback owner; command receipt alone cannot be reported as successful application. Verify the resulting queue and actual continuing/changed playback. Do not claim success for unsupported operations: return an unavailable reason and show it. Guard repeated Play on the same target while startup is pending; emit one start and show Starting on <screen>… on the initiating item. Cover both local and remote paths, with explicit retry still possible after failure.

Minimum undo is P0 by user decision: replacing playback, removing an item and clearing a queue offer Undo for 10 seconds measured from the original user tap, not from acknowledgment. Capture prior item, actual position, queue and playback/config state before mutation. Undo during pending wake/load must cancel that attempt and prevent late responses or delivery from starting it. Undo after application restores the affected screen's previous item/position/queue; a live source returns to live edge rather than a stale position. Never undo newer unrelated playback; compare the operation/playback identity at the applying owner. Cross-device access to Put it back and screen notes remain P1, but origin-device undo of a remote action is in this minimum slice. Expiry/cancellation seams must be compatible with Task8 rather than implemented as a toast-only timer. Starting fresh remains confirmed, not presented as reversible.

Ownership: shared Media item-actions/dispatch integration and existing item-action consumers; additive Content result-row props; local/remote queue controller adapters; screen receiver queue handling; minimum undo state/UI and necessary application/API/shared-contract cancellation additions; focused tests and factual reference docs. Root owns acceptance docs/runtime harness. Unit RED/GREEN covers every verb/target/collection ordering, one-off isolation, duplicate pending Play, unsupported receiver responses, actual queue mutation, undo before/after acknowledgment, expiry from tap, late canceled delivery, newer playback preservation, live edge and restored state. Add actual-player integration tests where a mocked dispatcher would hide a broken junction. Run changed tests and relevant local/remote/receiver/dispatch regressions; backend changes require layer/composition gates. Root performs multi-context/office-only acceptance after the complete path is available. Report exact evidence and limitations; commit scoped files, no deploy.

## Task 5: One search and complete browse junctions

Read handoff Step 4, RQ-FIND-01–10 and taxonomy FIND.1a/1b/2a/3a/4a/5a/6a/8a/8b. Use the shared item-actions contract and aim from Tasks3/4. One search surface, result renderer and status presentation must provide the same capabilities on phone/tablet/laptop, from start/browse/details/local controls/remote controls. An obvious launch action everywhere and keyboard typing where appropriate open search with input focused; do not steal typing from another input. Choosing a target never launches a separate search. Remove the separate FleetPlayPicker search once its entry points use the common flow.

Confirmed browser RED: `tests/live/flow/media/media-app-search-journey.runtime.test.mjs` types Disclosure Day on a phone, chooses Video, and plays; Search media dialog disappears instead of retaining query/scope. Preserve search and narrowing after Play, Add to queue and Play on…, while actual playback occurs at the stated destination and confirmation remains visible. Closing search restores the underlying route, controls target, scroll and focus; reopening starts scope at All. Playback and the bridge-owned media element must survive these transitions.

Kinds and nested kinds are visible choices beside input and update results without retyping. Show arriving-source status while results stream, terminate it when sources complete, and name failed sources with retry. A failed source is not an empty successful source: report failure before any widening. If a selected kind has no matches, show all-kind matches under a divider naming the selected kind (for example Not in Audiobooks — from everything:); every row spells out its kind. A truly empty result offers spelling/browse help and never looks like loading. Reject late results from older queries/scopes and retry only the failed source without silently duplicating current results.

Browse reaches every catalog kind without typing, renders pictures or recognizable placeholders/title/kind, loads long lists on scroll, preserves scroll on back, and provides a real navigable parent trail. Collection parts stay in natural order. Collection Play/Shuffle/Add at the top show shared aim and invoke the common contract. Details open in one step without changing playback, show available picture/title/description/length/kind/progress truthfully, and retain the full verb set. Household-wide spot/history completeness remains the P1 dependency, not fabricated data.

Ownership: Media search/browse/details/fleet entry wiring and tests; additive Content result/scope/status components and API search/browse completion/error/pagination seams where needed; factual reference docs. Root owns acceptance ledger and live harness. Unit RED/GREEN covers entry-point parity, focus/return state, keep-open action matrix, scope reset and nested selection, streamed completion/failure/retry, zero-result widening order, stale responses, browse natural order/pagination/back scroll/trail and details nonmutation. Include real provider/controller integration rather than assertions on mocked action names alone. Run changed tests and existing Content/Admin combobox regressions after additive changes; root verifies real catalog/browser and appropriate failure injection at external transport boundaries. Commit scoped files and report evidence; no deploy.

## Task 6: One handle and one controls surface

Read handoff Step5, RQ-STEER-01–03/05–10/15–18 and corresponding P0 STEER criteria. Merge local NowPlaying and remote Peek presentation into one target-bound controls surface using `useSessionController(target)`, not merely two similar-looking copies. Preserve Task2 actual media observations/seek/generation/focus behavior, Task3 aim/move separation and Task4 queue/undo contracts. Target name and a one-step playback target switch remain visible; changing the controls target never changes aim.

The persistent handle covers local playback and the most recently sent-to/actively steered screen. Show title/picture/progress/play-pause/target during playing/paused and retained ready queue; remove only when nothing is held. Opening full controls hides duplicate controls for that same playback, while leaving an obvious way to return. The handle opens full controls plus queue in one tap and exposes Move to…; a remote success confirmation and house row open the same controls without time-limited access. Lock-screen/notification controls remain P1, not implicitly accepted here.

Control content/order/layout are the same for local and remote. Show unsupported controls disabled with a plain reason. Play/pause/skip must reflect actual state within2seconds or display that the change has not happened yet; disconnected commands say Not sent and cannot execute after reconnection (coordinate Task8 delivery expiry). Include on-demand drag/time preview and ±10seconds using actual position, live label/Go to live, volume slider plus large step buttons with level, and speed where supported. A single Stop pauses/ends actual playback, keeps the queue and says Queue kept: n items. Clear is separate and uses minimum undo. Video expand/shrink must fill viewport and preserve playback; expanded audio shows picture/title.

Queue current/up-next/count are explicit. Video/audio/photos support queue shape; live/camera do not pretend to have a queue/position. Reorder supports drag and discrete up/down; tapping an item plays it, remove/clear use10second undo. Repeat off/all/one and shuffle appear exactly once beside queue and never interrupt current playback. Shuffle excludes Play next priority items and restores original queue order when disabled. Receiver behavior must be observed through the common controller, not inferred from command dispatch.

Confirmed visual gap from Task2: ordinary NowPlaying repeats title/artwork below video and places transport below the fold behind the fixed mini player. Eliminate duplicate playback controls, ensure controls are reachable without overlay interception, and test actual viewport bounds rather than class names alone. Ownership: handle/shared controls/queue/peek presentation and target selection; required controller capability/config/queue fixes; receiver config/queue additions only where needed; related tests and factual reference docs. Root owns runtime harness/ledger. RED/GREEN tests cover target/state/capability matrix, no duplicate controls, retained-ready handle, actual volume/speed/seek/stop, live behavior, identical queue operations, shuffle restore, focus host continuity and the2second pending/not-sent boundary. Root verifies browser/office or virtual-device parity at phone/tablet/laptop widths; no whole-story acceptance for unsupported P1/P2 criteria. Commit scoped changes/report, no deploy.

## Task 7: Browser screens, truthful fleet state, origin and routine parity

Read handoff Step6, RQ-HOUSE-01–03/05, RQ-AUTO-01/03/04 and the minimum naming dependency used by PLACE.1a/5a. Give every browser using Media a stable registered screen identity derived from its existing clientId and a unique human-readable name. Include this device (marked as such), configured screens and other app browsers in the same roster. Use one authoritative publication path for browser session state: reuse DeviceStatePublisher fed by the local controller, documenting the ownership contract; do not also synthesize conflicting state from a second stream. Route commands through existing client-control:<clientId> and the common controller so sends, moves and routines reach the actual browser player. Persist identity/name across reload. Requirements O6 lifts minimum naming into P0: offer a skippable first-use name prompt with a sensible default, support setting/renaming a unique persisted name and explain aim once; rename history/routine warnings and full administration remain P1/P2.

Add origin to load requests and command envelopes, carrying initiating device identity or routine identity/name into the affected session. Preserve it through queue/start/move boundaries according to which action actually starts playback. Replace Task3's logged busy approximation with exact origin since last idle; connect active sent/steered playback to aim idle suspension. A repeated title is not proof of the same origin. Update shared contracts, backend application/API and receiver/browser publication tests together. Do not allow an untrusted ordinary client to impersonate another device or a routine through these fields; reuse the project's request/client identity boundary.

One house indicator appears once at every width, counts playing/paused screens and opens the house view. Rows show name/room/state, picture/title/progress when playing, sort playing first, and expose Pause/Stop/Move here directly plus full controls and Play on. Reuse shared aim/actions/controls/outcomes. Heartbeat idle as well as playing screens at30seconds or less; no snapshot does not mean Idle or Off. After2minutes without a report show uncertainty and last-heard time; a disconnected house connection marks the whole view and recovers without reload. Commands on uncertain screens warn about unconfirmed outcomes. Browser closure reports stopped when observed; network disconnection alone must not invent a Stop while the browser may still play. The browser user is told that playback is visible to the house.

Routine entry points must invoke the same start semantics and options as a person: item/queue/shuffled collection and volume on configured or browser screens, with common progress/outcomes. The same trigger repeated within10seconds starts once with no duplicate queue entries, including triggers without a supplied dispatchId. Deduplicate server-side by trigger identity plus target/content/options/window without conflating different operations. A later human action wins over a pending routine, and a reload cannot replay an old routine start. Reuse cancellation/expiry/identity contracts instead of re-enqueueing late commands.

Ownership: browser identity/registry/publication/client command receiver; fleet roster/indicator/rows/liveness; origin contracts and aim-origin integration; routine adapter/application/API idempotency and tests; factual technical docs. Root owns runtime/ledger. RED/GREEN covers stable identity, unique names, roster/self marking, actual browser command application, origin changes including same-title replacements, idle heartbeat/uncertain/disconnected states, closing versus network loss, inline actions and single indicator, routine options/dedupe window/human precedence/reload. Run layer/composition/contracts gates and relevant device/session/dispatch regressions. Root must verify full two-browser or office-only send/move/control/routine journeys, including earlierTask3dependencies; never mark a registered row or accepted HTTP response as playback success. Commit scoped changes/report, no deploy.

## Task 8: One outcome system and commands that cannot arrive late

Read handoff Step7, RQ-RELY-01–03/05/06 and RQ-STEER-07. Replace the dispatch tray and ad-hoc notices with one outcome provider consumed by all shared item actions, controls, moves and routines. Preserve per-attempt immutable retry from Task1, undo/cancellation from Task4 and origin/application acknowledgment from Task7. Local results confirm quietly; remote results name item and screen with screen-appropriate progress. Confirmation of an actual change must not be emitted merely because a command was accepted. Progress remains visible across navigation until a terminal outcome; new same-kind confirmations replace earlier ones rather than stacking.

Confirmed remote playback says Playing on <screen> with Steer it. An unconfirmed start says it may not have started, offers Steer it and Try again, and persists until dismissed. New unconfirmed notices replace older ones for the same screen; a matching later playing report clears the notice without claiming an unrelated newer item satisfied the original attempt. Multiple independent failures remain separately actionable, each retrying or redirecting exactly its own payload. Redirect uses one-off target selection without silently changing aim.

Observe actual local and sent/steered remote playback failure and skip transitions, naming the failed item, screen and replacement when auto-advance occurs. Show a problem sign on the corresponding handle until recovery. Announce all outcomes accessibly, including quiet local confirmation, without flooding readers with every progress tick. The same outcome presentation must work from all surfaces at every width.

Distinguish undelivered Not sent, delivered but application pending, actual application, failed application and unconfirmed/lost acknowledgment. Within2seconds remote transport either reflects the real result or says it has not happened yet. A command declared Not sent must never execute after reconnect: enforce attempt cancellation/expiry at delivery and at the actual receiver, retaining enough terminal identity to reject delayed duplicates. Do not relabel a possibly applied command Not sent merely because its acknowledgment was lost. Pending wake/load undo and newer human playback must defeat late delivery through the same contracts, not separate toast state. Do not impose a2second total deadline on legitimate screen wake-up; show accurate progress instead.

Ownership: shared outcome provider/presentation and consumers; controller failure observation; remote transport and backend/receiver expiry/cancellation/application-status contracts; focused tests/reference docs. Root owns ledger/runtime harness. RED/GREEN tests cover entry-point equivalence, confirmation replacement versus distinct failures, persistent unconfirmed clearing only on matching playback, exact retry/redirect, local and remote skip notice/recovery, cross-route announcements, disconnected send, lost ack, reconnect, delayed delivery/duplicates, canceled wake, newer playback and the2second pending boundary. Run related dispatch/controller/receiver and backend layer/composition gates. Root verifies network-loss and delayed-start browser journeys against actual players; a simulated transport-only pass is supporting evidence. Commit scoped changes/report, no deploy.

## Task 9: Paused recovery, partial reset and predictable navigation

Read handoff Step8 and RQ-RELY-07/09–11 with taxonomy RELY.7a/8a/9a/10a. Reload/crash recovery restores local item, actual saved position, queue, repeat, shuffle and the prior area with playback paused; never autoplay simply because it was playing before unload. Restore aim through Task3's idle policy, not a second persistence owner. Preserve explicit user Play after restore, including same-title generations and real media duration/capabilities. Validate malformed/older persisted state safely and avoid initial-render defaults overwriting a usable stored session. Durable physical receiver power-cut recovery is the P1 extension, not silently accepted here.

A brief network loss must not reload the app, discard search/navigation or interrupt reachable media; sustained reconnecting shows a quiet accessible note and clears on recovery. Expired or canceled remote commands do not replay. Distinguish restored user intent from proven receiver state and keep uncertainty visible until fresh state arrives.

Start fresh lives in this device's settings, one step from any area, including search/queue/remote controls. Before mutation it lists what's playing, queue, spot and aim, offers independent keep choices and asks for confirmation. Cancel changes nothing. Apply exactly the selected reset, preserving kept state and the identity/name/history that were not listed; never reset a remote screen because its controls happen to be open. Do not offer undo for this confirmed irreversible reset. Reconcile retained queue/current item/position combinations explicitly so no dangling current index or misleading paused item remains.

Primary navigation indicates the current area even in queue/remote controls. Re-selecting the current area goes to its top without pushing another history entry. Device/browser Back and on-screen Back each return one actual step; labels name the real destination. A popup consumes one back step and restores origin focus/scroll without changing aim or remounting playback. Search close retains the context established in Task5. Keep one navigation/history model instead of independent ad-hoc escape stacks.

Ownership: session persistence/restore and selective reset API; settings/reset UI; Media route/history/navigation/dialog integration; reconnect presentation; tests/reference docs. Root owns ledger/runtime. RED/GREEN covers playing and paused reload, queue/repeat/shuffle/position, expired aim, corrupted storage, restore-before-save ordering, all keep/reset combinations, cancel and remote-view isolation, actual paused media then explicit resume, brief/sustained network loss without reload, tab re-tap and nested popup/back/focus/scroll. Root verifies ordinary reload/back/reset in real browser journeys and actual media pause, not persisted JSON alone. Commit scoped changes/report, no deploy.

## Task 10: Accessible parity, measurable budgets and P0 acceptance

Read handoff Steps9/10, RQ-RELY-13–15, NF-TAP/TIME/DEV/A11Y/REL and taxonomy P0 persona paths. This is a repair-and-verification slice, not a report that hands known gaps back to the user. Carry every earlier P0 dependency and failing/unverified criterion from the acceptance ledger into concrete end-to-end checks. Preserve explicitly P1/P2 criteria without claiming a mixed-priority story complete before all its criteria pass. Root owns the final acceptance matrix, live journeys and release gates; implementation worker owns accessibility/layout/performance corrections and focused regression tests across already-built surfaces.

Make every function usable at360px phone portrait/landscape, tablet and desktop using touch/mouse/keyboard. Minimum44×44px targets, text contrast4.5:1, controls/status3:1, large-text reflow without clipping, reduced motion and non-colour-only status. Announce confirmations/warnings/state changes; verify focus order/return/traps and meaningful names, not just automated accessibility scans. Aim, search and play/pause must remain in phone thumb reach without two-hand-only gestures. All hold/drag actions have keyboard/discrete equivalents. Remove superseded components only after confirming their callers use the shared surfaces and preserving Content/Admin compatibility.

Measure NF-TAP with ordinary input from the stated initial state: typed item→playing1tap, typed collection inlinePlay1, last-sent pause1, heldqueue1, add/playnext2, local aim2, Movehere2, retry1, oneoffsend3. Pauseall2 and cross-devicePutitback1 are later P1 budgets. Count menu opening but not typing. Verify actual destination/player/outcome after the final tap; lower click count alone is not a pass.

Measure release-bundle timing separately from dev-module transforms: visible tap response≤100ms, local confirmation≤500ms, remote result or pending≤2s, first remote progress≤1s, first streamed results≤1s for two-character query on home network, available local content starts≤3s, warm start usable≤1s, move position tolerance≤2s. Record setup/content/server conditions and measured values. Do not silently raise budgets or turn a request acknowledgment into actual-start timing. Fix app-caused failures; distinguish external availability failures explicitly.

Verify individual offline screens/source failures/server errors leave other functions usable, reachable local media continues while search/browse fail, queues of500items remain responsive, long-lived tabs do not leak subscriptions/timers/player nodes, and concurrent controllers cannot let stale commands beat a later action. Walk Seeker, Big-Screen Sender, House Watch, Hand-Held Viewer, Room Hopper and Fixer journeys on phone/laptop with two browser devices or Office only. Root performs actual multi-context state/command/player reconciliation, including canceled/lost-ack paths, rather than accepting mocked stores.

Run focused RED/GREEN before each correction and relevant full unit/contracts/flow/gates after source freezes. Record tests that did not reach preconditions as not verified, never passed/skipped acceptance. Root updates criterion evidence, technical docs and refactor status; performs whole-branch review and integration only after P0 criteria are genuinely satisfied. Build/deploy use the local gate as a separate halting step before build and again before deployment, verify served commit, exercise the current bundle and collect logs; preserve/restore Office and leave other kiosks untouched. An occupied household gate blocks deployment but not safe remaining implementation or isolated verification. Commit worker-owned changes/report only; deployment remains root-owned.

## Remaining sequence

11. P1 household history/spots/favourites, shared undo/notes, timers/queue end, multiple screens, system controls, durable receivers.
12. P2 suggestions/history, brief overlays, slideshow music, tracks, admin, screen power/alignment.

Expand each remaining slice into exact implementation/test tasks before executing it. Requirements and acceptance criteria are binding; no scaffolding-only completion.
