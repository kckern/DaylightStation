
  1. Make meal logging immediate
      - Automatically show the current meal’s empty section.
      - Put one microphone per meal, handling new foods, amendments,
        and grouping.

      - Fix the plus-button flow and stop opening irrelevant catalog
        suggestions by default.

      - Retire unused automatic sections as time advances; preserve
        logged food.

  2. Give voice input the right context
      - Selected foods narrow the target; otherwise, use that meal’s
        entries and recent input.

      - Apply clear instructions immediately with Undo.
      - Ask only when ambiguous, using selectable choices.
      - Fix the amendment path that created a new log for your potatoes
        correction.

  3. Add food grouping
      - Manual checkbox selection → Group → name the dish.
      - Voice grouping through the meal microphone.
      - Smart grouping button: selected foods only when selected;
        otherwise, the whole meal.

      - Preview smart suggestions before applying.
      - Create a parent with totals derived from its children, without
        double-counting.

      - Allow membership edits and ungrouping while preserving foods.

  4. Fix analysis feedback
      - Add animation and approximate progress.
      - Switch to animated diagonal stripes if processing exceeds the
        estimate.

      - Clear the card on actual completion; retain actionable errors
        and Retry.

      - Fix completed responses leaving an old “Analyzing” notice
        visible.

  5. Refine food rows
      - Order: density chip → icon → food name, with the icon close to
        the text.

      - Use nine discrete density colors and an actual decimal value,
        including 0.0, instead of a range.

      - Remove repeated “Estimated” labels.
      - Center the expansion triangle and size its visible shape to
        capital-letter height.

      - Preserve the approved compact spacing and dimmer, indented
        children.

  6. Fix daily summary cards
      - Give Budget, Food, Exercise, and Under/Over consistent visible
        cards.

      - Keep values and units on the same line.
      - Display whole numbers throughout the summary, including macros.
      - Preserve the approved colors, equal number sizes, compact two-
        column layout, and no daily density card.

  7. Verify the complete flows
      - Dinner capture, contextual amendments, grouping/ungrouping,
        Undo, slow analysis, and failures.

      - Confirm grouping preserves daily totals and display rounding
        preserves underlying precision.