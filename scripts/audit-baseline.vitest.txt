# GATE-VITEST baseline — the SET of vitest files currently failing.
# Population: *.test.{js,jsx,mjs} under tests/unit, tests/isolated,
# backend/ and frontend/ that vitest OWNS (explicit vitest import, or bare globals with
# no runner import), minus suite/ (jest) and node:test files.
# A file failing that is NOT listed here is a REGRESSION (gate exits 1).
# Captured: 15176 tests, 15079 pass, 42 fail.
# Regenerate with: node scripts/gate-vitest.mjs --update
#
# 2026-08-26: StockfishEngineAdapter.test.mjs was REMOVED from this list by
# hand rather than by --update. Its flake was diagnosed and fixed (an absolute
# wall-clock budget replaced by a control-relative one), verified to pass under
# 14-way CPU contention and to still fail when the regression it guards is
# re-introduced. Removing it means a future regression there is reported as a
# regression instead of being absorbed as "expected".
#
# A blanket --update was deliberately NOT run: four other files below
# (RenderPrintDocument, piano.courses, scripture, infrastructure-ownership)
# also passed that run, but they are UNVERIFIED — two of them are named in
# gate-vitest.mjs's own header as files that have flaked before. Protecting a
# file that merely happened to pass turns the next flake into a red gate, which
# is the habit this baseline exists to prevent. They stay listed until someone
# does for them what was done for Stockfish.
backend/src/1_adapters/fitness/YamlWorkoutRepository.test.mjs
backend/src/1_adapters/glossika/LegacyDumpReader.test.mjs
backend/src/3_applications/school/documents/RenderPrintDocument.test.mjs
backend/src/4_api/v1/agents/mountAgentHttp.test.mjs
backend/src/4_api/v1/agents/wireFormats/native.test.mjs
backend/src/4_api/v1/routers/piano.courses.test.mjs
backend/src/4_api/v1/routers/piano.effect-audit.test.mjs
backend/src/4_api/v1/routers/piano.loop-manifest.test.mjs
tests/isolated/adapter/content/filesystem/resolvers/scripture.test.mjs
tests/isolated/assembly/infrastructure/infrastructure-ownership.test.mjs
# Added 2026-08-27 when frontend/ entered the population. Pre-existing and
# deterministic: the rail name-floor assertion is `2 < 2` at the exact
# boundary, so it reads as a cushion rather than a measurement. It fails
# in isolation, is unrelated to the gate change, and belongs to whoever
# owns Surround/band — fixing a design tolerance as a rider on a gate
# change would be the wrong call. Drop this line when it is fixed.
frontend/src/modules/Surround/band.measure.test.jsx
