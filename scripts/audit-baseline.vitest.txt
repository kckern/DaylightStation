# GATE-VITEST baseline — the SET of vitest files currently failing.
# Population: *.test.{js,jsx,mjs} under tests/unit, tests/isolated, backend, frontend that vitest
# OWNS (explicit vitest import, or bare globals with no runner import),
# minus suite/ (jest) and node:test files.
# A file failing that is NOT listed here is a REGRESSION (gate exits 1).
# Captured: 31070 tests, 30975 pass, 40 fail, 52 skipped.
# --update PRESERVES the hand-written `#` prose below: a comment block
# directly above a path travels with that path; a block closed by a bare
# `#` is kept in the notes section. Nothing here records WHICH run wrote a
# line, so say in prose why an entry is here if the reason is not obvious.
# Regenerate with: node scripts/gate-vitest.mjs --update
#
# 2026-08-26: StockfishEngineAdapter.test.mjs was REMOVED from this list by
# hand rather than by --update. Its flake was diagnosed and fixed (an absolute
# wall-clock budget replaced by a control-relative one), verified to pass under
# 14-way CPU contention and to still fail when the regression it guards is
# re-introduced. Removing it means a future regression there is reported as a
# regression instead of being absorbed as "expected".
#
# A blanket --update was deliberately NOT run: five of the files below
# (YamlWorkoutRepository, RenderPrintDocument, piano.courses, scripture,
# infrastructure-ownership) also passed that run, but they are UNVERIFIED —
# two of them are named in gate-vitest.mjs's own header as files that have
# flaked before. Protecting a file that merely happened to pass turns the next
# flake into a red gate, which is the habit this baseline exists to prevent.
# They stay listed until someone does for them what was done for Stockfish.
# The case for waiting got STRONGER, not weaker, on 2026-08-27: the population
# grew 80% when frontend/ entered it, so one green observation now carries less
# evidence about contention than it did when the run was half the size.
#
# 2026-08-27: frontend/src/modules/Surround/band.measure.test.jsx is NOT listed
# here, though it was red when frontend/ entered the population. That file
# carries 102 assertions and exactly ONE of them failed; this ratchet is
# file-granular, so baselining it would have absorbed the other 101 as
# "expected" and unprotected a 3,000-line measurement suite to close a
# one-assertion hole.
#
# It was briefly marked `it.fails` and is now simply GREEN. The cause was not a
# padded constant: `Mar…` measures 38.3px on the machine the constant was
# derived on and 37.00px here, same font binary and rule, because Chromium's
# rasterised advance widths differ between builds. `SEGMENT_NAME_RUN_PX` is the
# ceiling of the widest reading, so `ceil(38.3) - 37.00` is exactly 2.00 and the
# test's `< 2` cushion band called the spread padding. The band is 3px now, the
# shipped constant is unchanged at 39, and the hard floor assertion stays exact.
#
# 2026-09-01: the list below is now EMPTY. All ten entries were run
# individually and pass; four needed real fixes rather than merely happening to
# go green, which is the verification the Stockfish note above asks for before
# an entry is removed by hand:
#   LegacyDumpReader   — `byUser.parent-two[0]` parsed as a SUBTRACTION
#                        (`byUser.parent` minus `two[0]`) and threw a
#                        ReferenceError before it could assert.
#   native / mountAgentHttp — parseRequest grew `messages`/`threadId` and
#                        respondError now rethrows so errorHandlerMiddleware
#                        owns the status (the `api-handrolled-500` audit rule).
#                        The tests still asserted the older shapes.
#   piano.loop-manifest — the route called `ds.getLoopManifest`, which was never
#                        implemented, so GET /api/v1/piano/loop-manifest
#                        answered 500 in production to a live `useLoopLibrary`
#                        consumer. It now serves an empty library and warns.
# The other six (YamlWorkoutRepository, RenderPrintDocument, piano.courses,
# piano.effect-audit, scripture, infrastructure-ownership) pass unmodified.
# They are the ones the note above flagged as UNVERIFIED under a blanket
# --update; each was run on its own here, which is the evidence that was
# missing. If one flakes under contention it should be re-listed WITH the
# observation that put it back, not restored silently.
#
#
# 2026-09-01 (second pass): the gate itself had been BROKEN — a single vitest
# spawn over the whole population stopped producing a JSON report past ~2,600
# files, exiting 249 with an empty stderr. It is chunked now and runs all 2,874
# files (31,070 tests). That is why this list is not the old one: these 13 are
# pre-existing failures the gate could not report while it was dead, not new
# breakage. Each still needs its own triage; they are recorded so the ratchet
# protects against NEW regressions in the meantime, which is the whole point of
# a baseline.
#
# registryCompleteness.test.mjs was in the captured run and is NOT listed: it
# was fixed while that run was in flight (state-gates is allowed a
# registered-but-absent config; the unregistered requirements/config.yml was
# retired) and verified passing by hand afterwards.
#
backend/src/3_applications/piano-games/OpponentDialogueService.test.mjs
backend/src/3_applications/playback-hub/runtime/HubStatusBroadcaster.test.mjs
backend/src/3_applications/school/LanguageStudyService.test.mjs
backend/src/4_api/v1/routers/config-context-boundaries.characterization.test.mjs
backend/src/4_api/v1/routers/piano.economyHook.test.mjs
backend/src/4_api/v1/routers/piano.routes.contract.test.mjs
backend/tests/unit/adapters/harvester/fitness/FitnessSyncerHarvester.test.mjs
backend/tests/unit/api/donow.approvalsAuth.test.mjs
backend/tests/unit/applications/admin/appsConfigService.registry.test.mjs
backend/tests/unit/applications/admin/yamlConfigFileService.allowlist.test.mjs
backend/tests/unit/applications/ambient/AmbientSchedulerService.test.mjs
backend/tests/unit/applications/devices/ScreenContentTracker.test.mjs
