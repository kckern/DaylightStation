# Household Integration Configuration Example
# Copy relevant sections to your integrations.yml

# Canvas art display providers
# Multiple sources can be configured - art will be aggregated from all
canvas:
  - provider: immich
    library: art           # Immich external library name containing art
  - provider: filesystem
    path: /media/art       # Local path with category subfolders (landscapes/, abstract/, etc.)
