# DaylightStation Local Development Environment
# Copy this to .env and set your values

# Required for local development
# Path to your config directory (containing config.app.yml, config.secrets.yml)
DAYLIGHT_CONFIG_PATH=/path/to/your/config

# Path to your data directory (containing fitness/, users/, etc.)
DAYLIGHT_DATA_PATH=/path/to/your/data

# Optional: NAS mount detection
# If set, the app will check if this path is accessible before loading config
# DAYLIGHT_NAS_MOUNT=/Volumes/your-nas-mount

# Optional: SMB share for auto-mount (macOS only)
# If set and NAS is not mounted, the app will attempt to open this share
# DAYLIGHT_SMB_SHARE=//your-nas-ip/share-name
